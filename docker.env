# 使用 Supabase 專案的直接連線 - 強制 IPv4
DATABASE_URL=${DATABASE_URL}
PORT=8080
OPENAI_API_KEY=${OPENAI_API_KEY}

# IPv4 强制选项
DATABASE_HOST=${DATABASE_HOST}

# 注意：實際部署時請在Railway設定這些環境變數：
# DATABASE_URL=your_actual_database_url
# OPENAI_API_KEY=your_actual_openai_key
# DATABASE_HOST=your_actual_host

# 如果你有實際的密碼，請取消下面這行的註解並填入正確密碼：
# DATABASE_URL=postgres://postgres:Kyy850425@db.vdpscuywgjopwvcalgsn.supabase.co:5432/postgres?sslmode=require 